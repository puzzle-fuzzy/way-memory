<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Raw route data and the active-session recovery marker are sensitive. -->
    <exclude domain="file" path="capture-queue" />
    <!-- The token ciphertext is tied to the Android Keystore on this device. -->
    <exclude domain="sharedpref" path="way-memory-credentials.xml" />
</full-backup-content>
